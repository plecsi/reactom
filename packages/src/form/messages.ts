import { defineMessages } from 'react-intl';

export const scope = 'admin.form';

export default defineMessages({
  submit: {
    id: `${scope}.submit`,
    defaultMessage: 'Submit kecske',
  },
  cancel: {
    id: `${scope}.cancel`,
    defaultMessage: 'Cancel kecske',
  },
});
