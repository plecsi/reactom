import { defineMessages } from 'react-intl';

export const scope = 'admin.FormBuilder';

export default defineMessages({
  savedForms: {
    id: `${scope}.savedForms`,
    defaultMessage: 'Saved Forms',
  },
});
